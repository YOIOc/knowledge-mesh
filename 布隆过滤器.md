## 1.数据结构
1. 二进制数组：数组只存储0/1数值（对应Redis中的bitMap数据结构）
2. 无偏hash函数：无偏函数就是能把元素的hash值计算的比较均匀的hash函数，能使计算后的元素下标较为均匀的映射到二进制数组中
如下就是一个简单的布隆过滤器，其中k1、k2代表增加的元素，a、b、c表示无偏hash函数，最下层表示二进制数组
![](assets/布隆过滤器/file-20260820223952741.png)
